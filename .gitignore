# dependencies
node_modules
.pnp
.pnp.js

# testing
coverage

# next.js
.next/
out/

# production
build

# misc
.DS_Store
*.pem

# debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# local env files
.env
.env.local
.env.development.local
.env.test.local
.env.production.local
# backups locales de .env — JAMÁS commitear credenciales del VPS o de cuentas
.env.*-backup
.env.*-backup-*

# vercel
.vercel

# typescript
*.tsbuildinfo
next-env.d.ts

# n8n workflow exports (pueden contener API keys)
San Luca AI WhatsApp*.json
*workflow*.json

# Backups locales y experimentos (no son código fuente)
*.bak
*.backup
*.backup-*
/src/

# Imágenes subidas desde el CRUD de platillos (se crean en el servidor)
/public/uploads/

# Herramientas de editor / IDE / agentes de IA (config PERSONAL de cada dev, no del proyecto)
.claude/
.agents/
.junie/
.idea/
.vscode/
skills-lock.json
*.code-workspace

# Dumps de base de datos (datos reales de prod — NUNCA al repo)
*.dump
*.sql.gz
*.sqlc

# Exports de Obsidian / notas y presentaciones sueltas (no son código)
docs/obsidian-export/
presentacion-*.md

# Instrucciones locales de Claude Code (si tu equipo las quiere COMPARTIR, borra esta
# línea y commitea CLAUDE.md a propósito; por defecto se trata como personal)
CLAUDE.md
